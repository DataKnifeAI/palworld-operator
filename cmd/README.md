# Manager entrypoint

Add `main.go` in TASKS.md C1 using the same controller-runtime manager setup as windrose-operator `cmd/main.go` (scheme registration for PalworldServer + Gateway API + Envoy Gateway types).
